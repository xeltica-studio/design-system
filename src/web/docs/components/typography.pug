extends _layout

block main
  h1 タイポグラフィ
  h2 見出し
  table.table
    thead
      tr
        th 構文
        th 例
    tbody
      each i in [1, 2, 3, 4, 5, 6]
        tr
          td: code &lt;h#{i}&gt;&lt;/h#{i}&gt;
          td: #{'h' + i} 見出しレベル#{i}

  h3 ディスプレイ見出し
  p
    code display
    | クラスを付与することで、より大きなサイズの見出しにできます。

  table.table
    thead
      tr
        th 構文
        th 例
    tbody
      each i in [1,2,3,4,5,6]
        tr
          td: code &lt;h#{i} class="display"&gt;&lt;/h#{i}&gt;
          td: #{'h' + i}.display 見出しレベル#{i}

  h2 文字サイズ
  table.table
    thead
      tr
        th 構文
        th 例
    tbody
      - const sizeList = [ 400, 350, 300, 250, 200, 150, 100, 75, 50 ];
      each size in sizeList
        tr
          td: code &lt;span class="text-#{size}"&gt;&lt;/span&gt;
          td: span(class=`text-${size}`) #{size}%

  h2 その他

  table.table
    thead
      tr
        th 構文
        th 例
    tbody
      tr
        td: code &lt;a href="#"&gt;リンク&lt;/a&gt;
        td: a(href="#") リンク
      tr
        td: code &lt;code&gt;Hello, XelticaUI!&lt;/code&gt;
        td: code Hello, XelticaUI!

  h2 リスト

  ul
    li Lorem, ipsum dolor.
    li Lorem, ipsum dolor.
    li Lorem, ipsum dolor.
    li Lorem, ipsum dolor.
    li Lorem, ipsum dolor.

  ol
    li Lorem, ipsum dolor.
    li Lorem, ipsum dolor.
    li Lorem, ipsum dolor.
    li Lorem, ipsum dolor.
    li Lorem, ipsum dolor.

  h2 引用

  blockquote あのイーハトーヴォのすきとおった風、夏でも底に冷たさをもつ青いそら、うつくしい森で飾られたモリーオ市、郊外のぎらぎらひかる草の波。
  blockquote: blockquote: blockquote: blockquote: blockquote: blockquote: blockquote: blockquote TEST

  h2 kbdタグ

  p
    kbd F1
    | を押下するとヘルプを表示します。

  p
    kbd
      kbd Ctrl
      | +
      kbd V
    | を押下すると、クリップボードから貼り付けます。
