include _header
include _footer

-
  const grayscales = [
    ['black', true],
    ['gray-12', true],
    ['gray-11', true],
    ['gray-10', true],
    ['gray-9', true],
    ['gray-8', true],
    ['gray-7', true],
    ['gray-6', false],
    ['gray-5', false],
    ['gray-4', false],
    ['gray-3', false],
    ['gray-2', false],
    ['gray-1', false],
    ['white', false],
  ];

  const colors = [
    ['red', true],
    ['vermilion', true],
    ['orange', false],
    ['yellow', false],
    ['lime', false],
    ['green', false],
    ['teal', false],
    ['cyan', false],
    ['skyblue', false],
    ['blue', true],
    ['indigo', true],
    ['purple', true],
    ['magenta', false],
    ['pink', false],
  ];

  const classes = [
    ['dark', true],
    ['light', false],
    ['disabled', false],
    ['divider', false],
    ['primary', false],
    ['success', false],
    ['info', false],
    ['danger', false],
    ['warn', false],
  ];

doctype html
html
  head
    meta(charset='UTF-8')
    meta(http-equiv='X-UA-Compatible' content='IE=edge')
    meta(name='viewport' content='width=device-width,initial-scale=1,minimum-scale=1')
    link(rel='stylesheet' href='/css/xeltica-ui.min.css')
    link(
      rel='stylesheet'
      href='https://use.fontawesome.com/releases/v5.15.3/css/all.css'
      integrity='sha512-iBBXm8fW90+nuLcSKlbmrPcLa0OT92xO1BIsZ+ywDWZCvqsWgccV3gFoRBv0z+8dLJgyAHIhR35VZc2oM/gI1w=='
      crossorigin='anonymous'
    )
    style.
      .xd-sidebar {
        width: 18rem;
      }
      .xd-footer {
        margin-top: 2rem;
      }
      .xd-belt {
        width: 100%;
        line-height: 2;
        text-align: center;
      }
    block head
    block title
      title XelticaUI

  body
    header.xd-header
      //- h1.display: a(href="/") XelticaUI
      //- p: b.text-danger 現在開発中のライブラリにつき、本番での使用は想定されていません。頻繁に仕様変更が行われること、機能が不十分であることをご留意ください。
      block header
        +header
    block body
    block footer
      .container
        +footer
    .card.fix-bottom-right-3: .hstack.dense
      button.btn.flat(tabindex="-1").xd-weather-btn: i.fas
      button.btn.flat(tabindex="-1").xd-share-btn: i.fas.fa-share-alt
    script.
      const shareButton = document.querySelector('.xd-share-btn');
      const shareText = `${document.title}\n${location.href}`;
      shareButton.addEventListener('click', () => {
        navigator.share({
          url: location.href,
          title: document.title,
        });
      });
      const weatherButton = document.querySelector('.xd-weather-btn');
      const cl = document.body.classList;
      if (localStorage.getItem('xelticaui:theme') === 'dark') {
        cl.add('dark');
      }
      weatherButton.children[0].className = cl.contains('dark') ? 'fas fa-moon' : 'fas fa-sun';
      weatherButton.addEventListener('click', () => {
        cl.toggle('dark');
        const currentTheme = cl.contains('dark') ? 'dark' : 'light';
        localStorage.setItem('xelticaui:theme', currentTheme);
        weatherButton.children[0].className = currentTheme === 'dark' ? 'fas fa-moon' : 'fas fa-sun';
      });
    script(src="/xeltica-ui.js")
    script.
      setupAll();

    block script
